# =============================================================================
# Tycoon Backend — Environment Variables
# Copy this file to .env and fill in the values.
# Variables marked REQUIRED have no safe default and MUST be set in production.
# =============================================================================

# ── App ───────────────────────────────────────────────────────────────────────
# NODE_ENV: development | staging | production | provision | test
NODE_ENV=development
PORT=3000
API_PREFIX=api
API_DEFAULT_VERSION=1
API_ENABLE_LEGACY_UNVERSIONED=true
API_LEGACY_UNVERSIONED_SUNSET=          # ISO date, optional
CORS_ORIGIN=http://localhost:3000
ENABLE_SWAGGER=false                    # set true only in non-prod

# ── Database ──────────────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=tycoon_db
# DB_SYNCHRONIZE: NEVER true in production — use versioned migrations only.
DB_SYNCHRONIZE=false
DB_LOGGING=false

# --- Connection Pool ---
# DB_POOL_SIZE: max open connections per app instance.
#   Local default: 5  (small; avoids exhausting a dev Postgres container)
#   RDS default:   20 (db.t3.medium has max_connections ~100; leave headroom for replicas)
# DB_POOL_SIZE=5

# DB_POOL_IDLE_TIMEOUT_MS: ms before an idle connection is closed.
#   Local default: 10000
#   RDS default:   30000  (must be < RDS idle_client_timeout 600 000 ms)
# DB_POOL_IDLE_TIMEOUT_MS=10000

# DB_STATEMENT_TIMEOUT_MS: hard limit on any single statement (0 = disabled).
#   Local default: 0  (disabled; useful for long seed/migration scripts)
#   RDS default:   30000  (kills runaway queries before they hold locks)
# DB_STATEMENT_TIMEOUT_MS=0

# DB_CONNECT_TIMEOUT_MS: how long to wait for a connection from the pool.
#   Default: 5000 (both local and RDS)
# DB_CONNECT_TIMEOUT_MS=5000

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRATION_TIME=1d
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
# Clock skew tolerance in seconds (default: 60)
# Allows tokens to be valid within this time window to account for clock differences
JWT_CLOCK_SKEW_SECONDS=60

# CORS Configuration
# CORS_ALLOWED_ORIGINS - Comma-separated list of allowed origins
# Examples:
#   Development: http://localhost:3000,http://localhost:3001
#   Production: https://app.example.com,https://admin.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# CORS_CREDENTIALS - Enable credentials (cookies, authorization headers)
# Default: true
# Note: Cannot use wildcard (*) origin when credentials are enabled
CORS_CREDENTIALS=true

# CORS_MAX_AGE - Preflight cache duration in seconds (Access-Control-Max-Age)
# Default: 86400 (24 hours)
# Reduces preflight requests by caching CORS policy
CORS_MAX_AGE=86400

# CORS_DEV_WILDCARD - Enable development wildcard rules
# Default: true
# When enabled in development, automatically allows:
#   - localhost (any port)
#   - 127.0.0.1 (any port)
#   - *.local domains
# Set to false to enforce strict allowlist even in development
CORS_DEV_WILDCARD=true

# Legacy CORS Configuration (deprecated, use CORS_ALLOWED_ORIGINS instead)
# CORS_ORIGIN - Single origin (backward compatibility)
# If CORS_ALLOWED_ORIGINS is not set, this will be used
# CORS_ORIGIN=http://localhost:3000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=                         # leave blank if no auth
REDIS_DB=0
REDIS_TTL=300

# ── Logging ───────────────────────────────────────────────────────────────────
# LOG_LEVEL: error | warn | info | http | verbose | debug | silly
# Defaults: debug (development), info (production), error (test)
LOG_LEVEL=debug
# LOG_CONSOLE: enable stdout logging in production containers
LOG_CONSOLE=false

# ── Payment / Webhooks  (REQUIRED in production) ──────────────────────────────
# PAYMENT_WEBHOOK_SECRET must be ≥ 16 characters in production.
PAYMENT_WEBHOOK_SECRET=                 # REQUIRED in prod — set in your payment provider dashboard

# ── Ledger Reconciliation ─────────────────────────────────────────────────────
# Force dry-run mode regardless of NODE_ENV (useful in staging CI)
RECONCILIATION_DRY_RUN=false

# ── Data Export (Privacy module) ──────────────────────────────────────────────
DATA_EXPORT_DIR=./storage/data-exports
DATA_EXPORT_TTL_HOURS=24

# ── Graceful Shutdown ─────────────────────────────────────────────────────────
# Maximum ms to wait for in-flight work before forcing exit.
# Must be < Kubernetes terminationGracePeriodSeconds * 1000 (default: 30 000 ms).
SHUTDOWN_TIMEOUT_MS=15000

# ── Game Defaults ─────────────────────────────────────────────────────────────
DEFAULT_AUCTION=true
DEFAULT_RENT_IN_PRISON=false
DEFAULT_MORTGAGE=true
DEFAULT_EVEN_BUILD=true
DEFAULT_RANDOMIZE_PLAY_ORDER=true
DEFAULT_STARTING_CASH=1500
